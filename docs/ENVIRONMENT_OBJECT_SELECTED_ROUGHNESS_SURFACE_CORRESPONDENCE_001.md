# Environment Object selected roughness surface correspondence 001

## Bounded question

Does the real current-world Map Object receiver expose the exact two source-owned service surfaces targeted by Object Materials' selected `service_dark` roughness field, before UV or texture adoption is attempted?

## Pinned authorities

- accepted multi-asset world: Map Environment head `7713cbe5863c3bc38dabb6236eb4b393401224b6`;
- real receiver readiness evidence: Map head `1fcc9727012d156f8d1c4658c8dca4f0e9dd3c33`;
- Object Hard-Surface service-surface owner: PR #26 head `f7c64d08e4e2a0d6954291d8b4e064d7345ab658`;
- selected Object Materials authority: PR #6 head `0515a2d5ad2c7a1eb545f2b7b327b7367530dfca`;
- current Map Object material-family receiver: Materials head `c85517446a769e0d5f880fc0e9e32f47124f7b5e`.

## Method

The verifier imports the exact Object owner's deterministic source generator instead of reimplementing it. It derives the exact two source faces from the source-owned selectors, checks source frames and metric-domain coverage, binds the exact Materials two-surface atlas contract, then compares those source triangle identities against the exact Object material partition retained in all 17 states of the accepted Building + Nature + Object + footprint + Weather world.

The previous real Godot readiness evidence is consumed unchanged. That evidence already proves 68 retained real scene frames, 1,224 Weather-width measurements and zero current receiver UV0 values. No new scene render is manufactured for a diagnostic-only provenance pass.

## Expected receiving diagnosis

The source owner resolves exactly four selected triangles total: two for `lid_inner_service_surface` and two for `front_service_panel_outer_service_surface`.

The current Map receiver is component-role partitioned rather than selected-face partitioned:

- the lid-inner target remains inside the whole `shell_coating` lid surface;
- the front outer target is only two triangles inside the twelve-triangle whole `service_dark` front-panel surface;
- the current receiver still has no UV0/TEXCOORD_0 stream.

Therefore the selected spatial roughness field is blocked by **two independent receiving gaps**: exact source-surface segmentation and exact UV identity.

## Decision boundary

Expected state:

`HOLD_CURRENT_WORLD_OBJECT_SELECTED_ROUGHNESS_RECEIVER__SOURCE_SERVICE_SURFACE_SEGMENTATION_AND_UV0_BINDING_NOT_PRESENT`

Environment does not assign the selected material, split production surfaces automatically, invent UVs, change material values, or adopt the Technical Art texture transport. The next receiver may be a bounded candidate that makes the exact two source-owned faces independently addressable while retaining current appearance, but Runtime cost and Materials / Technical Art / Art / QA authority remain separate.

## Reusable rule

`SPATIAL_MATERIAL_FIELD_REQUIRES_EXACT_SOURCE_SURFACE_SEGMENTATION_BEFORE_UV_BINDING_OR_TEXTURE_ADOPTION`

A spatial material field must bind to the exact source semantic region it claims to affect. A component-wide material name is not sufficient when the selected field targets only particular source-owned faces.

`axm-create-me` remains coordination-only. Repo identities are preserved. The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
