# Environment selected roughness + compact-east current-world composition 001

Status: **BOUNDED REVIEW CANDIDATE — DUAL ENVIRONMENT ADOPTION HELD**

## Question

Can the exact already-reviewable Object selected-roughness candidate coexist with the exact green compact-east Nature visual-response receiver in the retained real current world, without changing Building, west-sapling Nature, the Map footprint cue, Weather, route, cameras or lighting?

This is an Environment / World Art integration proof. It is not a new Object material, Nature VFX source, Runtime optimization, or Art/QA approval lane.

## Exact parents

Selected-roughness Environment parent:

- Map head `d8a1d950ed5f21e6ad356404f46407c99c160017`;
- workflow `35235994153`;
- artifact `10502588586`;
- archive SHA-256 `60c1e589e3e1c3a7dab5430b55ef59542dd7ca35dad8b13653d165c79eee3a1e`;
- state `PASS_CURRENT_WORLD_OBJECT_SELECTED_ROUGHNESS_APPEARANCE_CANDIDATE_REVIEW_READY__ENVIRONMENT_ADOPTION_HELD`.

Compact-east VFX receiving parent:

- Map head `29ef2d4cc4398b3f26290e4e1f1f10398ca9898c`;
- Nature VFX owner `cef2ad78d8e36a55ada5dad07329f1a7125d48de`;
- workflow `35243081614`;
- artifact `10506765610`;
- archive SHA-256 `3b7ee551d21b88e8568b88194a804459a77774d08103bc11f5b53756df9fd588`;
- state `PASS_CURRENT_WORLD_COMPACT_EAST_VISUAL_RESPONSE_TARGET_HOST`.

Both derive from the accepted current-world parent `7713cbe5863c3bc38dabb6236eb4b393401224b6`.

## Smallest receiver composition

The exact VFX observer body already knows how to rebuild the compact-east static source for every one of its 17 phases. Reimplementing that observer inside Environment would duplicate VFX receiving logic.

Instead, the evidence workflow pins the exact VFX observer Git blob `67a3aa1f1932a99de1b6e835ad308630bff282a5` and composes it over the exact selected-roughness receiver by changing only its first GDScript `extends` line:

- from `atmosphere_current_world_nature_leaf_flutter_observe.gd`;
- to `atmosphere_current_world_object_selected_roughness_observe.gd`.

Every donor line after that parent declaration must remain identical. The normal `super.add_static_source()` chain therefore still performs the existing exact Object selected-face/UV0/roughness binding, while the VFX body remains responsible for phase-by-phase compact-east rebuilding.

This keeps ownership separate: Environment owns only the receiving composition.

## Evidence gate

The combined target-host pass must prove all of the following at the same exact head:

- 17 retained current-world states and 68 matched target-host frames;
- the Object selected-roughness runtime observation is exactly equal to the selected-roughness parent in all 17 states;
- exact selected PNG identity `57cf746a9a7e0615884fe3c45c6c4df677c2bd0631def61b3ccb1684daa26949`;
- exact selected scalar identity `b8d13c07f9b71278042b0d42d44b84579a3f327c6adf6723cae4c8c8f06dd38e`;
- compact-east runtime phases `0..16` exactly match the green VFX receiver sequence;
- neutral compact-east endpoints remain pixel-exact to the selected-roughness parent;
- `elevated_oblique` retains visibility of all 15 interior phases in both Weather presentation modes;
- `path_eye` remains the legitimate non-observing compact-east camera;
- all 1,224 source-width Weather measurements remain inside the existing `0.05 px` gate;
- draw calls, objects and RenderingServer primitive counts remain unchanged relative to the exact selected-roughness parent.

Proof-host buffer/texture deltas are recorded as observations, not converted into device budgets.

A negative control mutates the selected-roughness identity in one combined runtime state. The verifier must reject it. This prevents a green Nature stack from silently dropping the Object material candidate it claims to preserve.

## Reusable rule

`CROSS_ASSET_WORLD_STACKING_SHOULD_HOLD_ONE_REVIEW_CANDIDATE_EXACT_WHILE_PROVING_THE_SECOND_AGAINST_THE_EXACT_ALREADY_REVIEWABLE_PARENT`

When two independently reviewed world-art candidates are stacked, keep one exact as the comparison parent and prove the second as the only new variable. Do not infer coexistence from two separate green branches.

## Adoption boundary

Even on PASS:

- selected roughness Environment adoption remains **false**;
- compact-east Environment adoption remains **false**;
- Object Materials and Technical Art retain their source/transport authority;
- Nature/VFX retain compact-east source/motion authority;
- Runtime retains device/performance authority;
- Art Direction and independent Visual QA retain appearance acceptance;
- Weather remains visual-direction-only, not physical wind;
- CANON and production readiness remain unclaimed.

The four AXM roots — Truth, Agency / non-domination, Continuity, Wisdom before speed — remain the merge gate.
