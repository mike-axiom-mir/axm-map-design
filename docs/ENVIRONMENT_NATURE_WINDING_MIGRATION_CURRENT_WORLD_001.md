# Environment Nature winding migration current-world receiving 001

## Scope

This bounded Environment / World Art successor keeps the existing current-world composition lane and receives two already-owned Nature changes into that real multi-asset scene:

- Nature Geometry PR #9 exact source-generator cap-winding migration at `4ddbe66e5c02d22407ef773d5346a2fe6f349a2d`;
- Nature VFX PR #11 exact migrated-topology sapling visual-response rebind at `0b9167ac6d7b6d94d9fef92720f8c60e3ef45700`.

The parent visual world remains the Environment PR #24 lineage with:

- Building header segmentation + accepted Building material values;
- exact five-surface Object receiver with the Runtime #33 indexed representation;
- the restrained visible west-Object footprint cue;
- Nature woody / foliage material family;
- source-width Weather presentation;
- existing path, cameras and lighting.

`axm-create-me` remains coordination-only. Product/evidence implementation stays in `mike-axiom-mir/axm-map-design` and Nature source authority stays in `mike-axiom-mir/axm-nature-design`.

## Integration discovery: the current world had mixed Nature lineage

The first exact receiving attempt correctly failed because it compared source-local Nature vertices directly against Map receiver vertices. Inspection showed that the current Environment payload stores the same Nature geometry after an existing constant world-space translation, not at the source-local origin. The corrected receiver therefore proves `receiver_vertex = current_source_vertex + existing_world_translation` within `1e-12 m` and never removes or rewrites that placement.

That comparison also exposed a useful cross-repo continuity fact:

- `sapling-neutral-001` still had the historical cap winding and needs exactly `260` triangle winding replacements;
- `compact-east-tree-neutral-001` still had the historical cap winding and needs exactly `260` triangle winding replacements;
- `east-rear-tree-neutral-001` is already on the migrated winding lineage through the earlier Environment rear-tree source work and therefore needs exactly `0` triangle changes.

Environment closes only those missing receiving edges. It does not re-migrate the rear tree and does not rewrite any Nature source JSON.

## Exact receiving rule

For the three current Nature sources, Environment accepts the migration only when:

1. the exact current Nature generator reproduces the PR #9 migrated source mesh digests;
2. each current receiver preserves its existing world-space translation and matches the exact migrated source/deformed vertices up to that one translation with maximum residual `<= 1e-12 m`;
3. every triangle preserves exact vertex membership while only winding may differ;
4. the observed winding delta is exactly `260 / 260 / 0` for sapling / compact east / east-rear respectively;
5. the existing woody / foliage material partition remains `520 / 50` triangles for every Nature receiver;
6. the 17-state sapling vertex sequence is reproduced by the exact PR #11 rebound response at the current Environment sample times, while the response profile remains unchanged;
7. Weather, Building, indexed Object, visible footprint dressing, path, cameras and lighting remain unchanged.

The Godot receiving pass then renders all `17 states x 2 cameras x 2 inherited Weather modes = 68` frames, preserves the exact `1,224` source-width Weather measurements, and requires the current proof-host draw/object/primitive/buffer/texture counter shape to remain unchanged. Retained frame differences caused by corrected winding, generated normals and culling are characterized, not declared visually neutral.

## Preserved failed drafts

Two failed runs remain part of the evidence chain rather than being rewritten away:

- run `35158672534` stopped in proof infrastructure because the runner did not yet have `pytest` installed;
- run `35158854956` passed the exact Nature source tests but rejected the current receiver because the first Environment verifier incorrectly treated the receiver's baked world translation as a source-geometry mutation. That false assumption was repaired; no source, visual or acceptance threshold was weakened.

The corrected placement-preserving receiver first completed end-to-end at Environment head `ebd0b95ebca55b98203a24e6862a39f89660c7dd`, workflow `35159148525`, with structure composition digest `50e3c3f10911514fe00a2431cc4c265e6539672d8e364063112f0b45660a9337` and target-host result `PASS_CURRENT_WORLD_NATURE_SOURCE_WINDING_MIGRATION_TARGET_HOST_REACHED`.

## Truth boundary

A green Environment result may prove only that the exact migrated Nature source lineage reaches the current multi-asset Godot proof while the declared unrelated scene identities and existing world placements remain fixed. It may not promote final Nature visual preference, arbitrary-camera behavior, global outward-normal correctness beyond the pinned migration evidence, final normals/tangents/UVs/textures/sidedness, physical wind, plant biomechanics, target-device performance, collision/navigation/gameplay, CANON or production readiness.

Art Direction and Visual Observer / QA retain perceptual acceptance. Runtime retains target-device cost. Nature retains source geometry and VFX authority. The four AXM roots — Truth, Agency / non-domination, Continuity, Wisdom before speed — remain the merge gate.
