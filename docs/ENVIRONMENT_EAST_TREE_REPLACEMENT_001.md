# Environment east-tree replacement proof 001

Status: bounded Environment / World Art evidence lane on PR #4.

## Question

Can the exact source-owned `compact-east-tree-neutral-001` from `mike-axiom-mir/axm-nature-design#6` replace the remaining seed-29 `proxy:nature-tree-east-b` inside the existing Environment receiving scene without hidden scale/rotation, path or spacing regression, or unrelated scene changes?

This comparison exists because Art Direction and Visual Observer both retained the large east-foreground Nature proxy as the strongest remaining scene-level hierarchy weakness after the west sapling motion gate passed.

## Exact dependency

- Nature repository: `mike-axiom-mir/axm-nature-design`
- Nature PR: `#6 — Organic Form: add compact east-foreground tree source study`
- exact Nature head: `64116d63fc76daa1623b5fd5046a4e6074100bda`
- source study: `compact-east-tree-neutral-001`
- source digest: `9c87cf26f02f7adee832908652942218ec779c9029a0611aae1fb66eb0f62f54`
- mesh digest: `c7367ed5dcea6ebe39869c48fd653845b25c9a8725a2e637a1d6f2fbee1fa32f`
- source form: `390` vertices / `570` triangles
- placement policy: `PRESERVE_TARGET_CENTER_XY__GROUND_SOURCE_MIN_Z__NO_FORM_SCALE__NO_EXTRA_ROTATION`

The baseline remains the exact seed-29 mixed Environment scene already proven on this PR: west source-owned sapling + source-owned Weather field + fixed `path_eye` and `elevated_oblique` cameras. Only the east Nature proxy is replaced in the candidate scene.

## Evidence contract

`axm.environment-east-tree-replacement-evidence/v0.1` must revalidate:

- the existing source-integration and fixed-camera payload first;
- exact target identity and `PROXY_ONLY` state;
- exact Nature source/evidence identity;
- exact no-scale/no-extra-rotation placement policy;
- source footprint and height inside the reserved proxy body;
- source grounded at scene Z=0;
- readable path remains unblocked;
- minimum spacing remains valid against the remaining proxies and the already-integrated west sapling;
- west sapling, Weather, cameras, path and every unrelated scene item remain byte-semantically unchanged;
- exactly one new source mesh replaces the target proxy.

The target host renders both baseline and candidate through the same pinned Godot 4.7.2 GL Compatibility `environment-proof/observe.gd` host. The host was extended only to accept an optional list of additional exact source meshes; the existing sapling/Weather/proxy paths remain backward compatible.

## Visual question

Retain fixed-camera before/after PNGs. Environment may record a scoped composition observation, but Art Director / Visual Observer remain the acceptance gate for whether the compact body actually reduces the foreground hierarchy/occlusion problem while retaining a useful Nature silhouette.

Do not mix Materials PRs, Weather retuning, object replacement, camera changes or additional placement changes into this first comparison.

## Truth boundary

A green structural + render gate establishes only that the exact source body can be composed and captured in this exact receiving scene under the declared no-scale placement contract while preserving the rest of the scene.

It does **not** establish final world art, botanical correctness, deformation/wind behavior, final Materials/lighting, collision/navigation/gameplay, runtime cost, Art Director/Visual Observer acceptance, CANON, production readiness, or Environment/Nature mastery.
