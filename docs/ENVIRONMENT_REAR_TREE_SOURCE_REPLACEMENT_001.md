# Environment rear/right Nature source replacement 001

## Scope

This Environment / World Art lane starts from exact Map PR #11 head `c72838eb4b40ee7903a3c3e326a1bf77fa08bee5`, where the accepted compact east tree and exact Building pavilion are already present in the retained seed-29 world. It replaces only `proxy:nature-tree-east-a` with exact source-owned Nature PR #8 body `east-rear-tree-neutral-001` at `a4e5ee011e1d87f47866a7e6c6f4e66f57b6af12`.

The lane is intentionally a sibling of Map Materials PR #14. PR #14 owns the pavilion material-in-context A/B; this Environment lane does not consume it or alter Building materials. That keeps the two highest-value receiving questions isolated rather than contaminating either comparison.

## Composition contract

The rear Nature candidate must preserve:

- retained procedural seed `29`;
- exact source-owned west sapling;
- accepted compact east tree;
- exact source-owned Building pavilion from PR #11;
- exact Weather field and presentation semantics;
- both Object proxies and all unrelated remaining proxies;
- readable path and both fixed cameras;
- existing neutral Environment proof materials and lighting.

Only `proxy:nature-tree-east-a` may disappear, and exactly one `source:nature:east-rear-tree-neutral-001` mesh may be added.

Placement is `PRESERVE_TARGET_CENTER_XY__GROUND_SOURCE_MIN_Z__NO_FORM_SCALE__NO_EXTRA_ROTATION`. The source must independently fit the exact retained slot `[1.678837, 1.678837, 4.197092] m`, preserve the declared path/spacing gates, and re-run Nature's own `PASS_REAR_SOURCE_ENVELOPE` evaluator from the exact source checkout.

## Topology truth boundary

This receiving proof deliberately consumes the established Organic baseline mesh digest `d7fc5deaa1c12d1d8c7d7b6dc95bf1e8544ce26140ee2e4a7d2c67a2c4133e48` rather than silently adopting Geometry PR #7's derived reindex-only candidate. Geometry has separately proven the shared Nature tapered-cap winding repair, but source migration is still held.

The proof host therefore renders the rear tree with culling disabled for observation only. A visible target-host result must not be relabelled as target-engine culling/topology acceptance.

## Evidence gate

The dedicated workflow:

1. binds the actual branch/PR head rather than GitHub's synthetic merge checkout;
2. checks out exact baseline Nature, compact Nature, rear Nature, Weather and Building dependencies;
3. rebuilds PR #11's exact accepted Building scene first;
4. runs the isolated rear-tree structural replacement contract and fail-closed regressions;
5. renders baseline/candidate through pinned Godot `4.7.2` GL Compatibility using the existing Environment observation host;
6. requires the rear source to be present as `390` vertices / `570` triangles in both retained camera receipts;
7. requires a visible `elevated_oblique` target-host delta and retains both cameras for direct Art Direction / Visual QA review.

## Non-claims

A structural/render PASS proves only that this exact source can replace this exact retained proxy while the accepted multi-asset world state remains intact and that the change reaches the pinned proof-host image.

It does not prove final visual hierarchy, botanical correctness, source topology migration, authored normals/tangents/UVs, vegetation materials, deformation/wind quality, collision/navigation/gameplay, target-device FPS/GPU/memory budgets, final world art, CANON, production readiness, game readiness, or Environment/Nature mastery.

`axm-create-me` remains coordination-only. Source authority remains in Nature; Map owns receiving composition. The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
