# Environment Live Object Current World 001

Status: **EVIDENCE CANDIDATE / ENVIRONMENT RECEIVING COMPOSITION ONLY**

This lane closes one specific sibling-branch gap in the current Map evidence. Environment PR #19 proves the exact west Object proxy can be replaced by the exact source-owned `modular-equipment-case-001`; VFX PR #20 separately proves the exact 17-state Weather + west-sapling sequence in the current migrated-rear-tree world and now has independent Visual QA clearance for the rear-tree culling regression question. Neither lane proves the two current improvements together.

## Bounded composition

This branch stacks exactly on VFX PR #20 head:

`3e641a5ea7b2507a53e5ff1a8fba0f0f9c94abaf`

It rebuilds the exact Environment/Object donor from PR #19 head:

`85a2a0be959b4e39f9ffcf83da89078b604f9881`

The Object structural source remains exact:

- Object head `d3fa10a270faae7925811f44f03381fe5c5d0215`;
- source SHA-256 `49b1f9ed9865893d6de6f1ec8f069576732df694853fde4e3fcff366de32644a`;
- generated OBJ SHA-256 `3e01ef3bf4935ee6aee7c56c03dc0b7f54c308e5ac2eb6a7583252a366901106`;
- `468` vertices / `812` triangles;
- exact PR #19 west-slot placement and no-scale/grounding policy.

No Object material, lid/latch articulation, service-module attachment, UC scene graph, runtime controller or gameplay semantics are consumed.

For each of the exact 17 PR #20 atmosphere states, the Environment composer:

1. preserves the exact Weather field and west-sapling state/digests;
2. removes only `proxy:object-crate-west`;
3. adds exactly the PR #19 source-owned Object world mesh and placement metadata;
4. preserves `proxy:object-crate-east` as the remaining proxy/control;
5. preserves the exact Building pavilion;
6. preserves all three source-owned Nature bodies and the migrated rear/right tree `CULL_BACK` review state;
7. preserves path, cameras, Weather presentation and all unrelated receiving state.

The existing PR #20 Godot 4.7.2 observer is reused unchanged through an explicit compatibility projection. The canonical Environment evidence retains its own schema and truth boundary; the projection exists only to reuse an already-proven observer rather than fork another renderer script.

## Evidence intent

The structural gate must prove the PR #19 static replacement composes into all 17 PR #20 states without changing their dynamic Weather/sapling identities or any unrelated receiving state.

The target-host gate must prove:

- all 17 states render;
- all 34 retained frames exist and remain distinct per fixed camera through the inherited PR #20 gate;
- the exact Object source is present in every runtime sample as `468 / 812`;
- that Object source remains `CULL_DISABLED` for this Environment proof;
- the migrated rear tree remains `CULL_BACK`;
- the unchanged PR #20 target-host gate remains green.

This is deliberately a convergence proof, not another source-authoring lane.

## Why this is the current Environment gap

The constellation already has active owners for:

- Nature terminal-leaf/backface representation;
- Object articulation, UC hierarchy and runtime resource lifetime;
- Building materials/procedural authoring;
- Weather/VFX motion;
- Runtime budgets and final Art Direction / independent QA.

Environment therefore should not invent another tree, prop, material or effect. The highest-leverage unoccupied world-art task is to stop the two newest Map receiving improvements from remaining mutually exclusive sibling branches.

## Truth boundary

A PASS proves only exact receiving composition and pinned target-host visibility for the already-proven static Object source replacement inside the already-proven dynamic current-world atmosphere sequence.

It does **not** prove final Object materials, lid/latch motion, service-module attachment, collision/physics, dynamic Object interaction, physical wind, renderer interpolation, wall-clock playback, target-device FPS/GPU/VRAM budgets, final environment dressing, final Art Direction, gameplay/navigation, CANON, production readiness, or Environment / World Art mastery.

`axm-create-me` remains coordination-only. Object keeps Object source authority, Nature keeps Nature source/topology authority, Building keeps Building source authority, Weather/VFX keep atmosphere authority, Map owns only receiving composition, and the four AXM roots remain the merge gate.
