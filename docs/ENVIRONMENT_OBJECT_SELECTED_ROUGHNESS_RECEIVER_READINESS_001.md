# Environment Object selected-roughness receiver readiness 001

## Scope

This is a bounded Environment / World Art receiving check on the existing Map PR #24 lane. It does not author another Object roughness model, another UV layout, another Object mesh, another Technical Art transport path, or another Runtime storage candidate.

The question is narrower:

> Can the exact Art-preferred `service_dark` roughness field that Materials has frozen and Technical Art has transported through merged UC be truthfully adopted by the exact current-world Map Object receiver today?

## Exact inputs

Current accepted real-scene receiving surface:

- Map Environment evidence head `7713cbe5863c3bc38dabb6236eb4b393401224b6`;
- real Godot 4.7.2 current-world evidence from workflow `35174899697`;
- 17 retained states / 68 rendered observations;
- Building + Nature + indexed Object + visible Environment footprint cue + Weather present together;
- 1,224 retained Weather projected-width measurements.

Current Map Object receiver:

- Object source head `d3fa10a270faae7925811f44f03381fe5c5d0215`;
- source SHA-256 `49b1f9ed9865893d6de6f1ec8f069576732df694853fde4e3fcff366de32644a`;
- five current Environment material surfaces: `shell_coating`, `service_dark`, `hardware_steel`, `rubber_guard`, `interface_orange`;
- current Environment Materials identity `c85517446a769e0d5f880fc0e9e32f47124f7b5e` / profile SHA-256 `dc200229d6c25fa84063aa51f66103abc022efa54b2167e4432a5b47fc40360c`;
- receiver geometry is rebuilt by `SurfaceTool`, normal-generated and post-normal indexed.

Exact selected roughness authority:

- Object Materials PR #6 head `0515a2d5ad2c7a1eb545f2b7b327b7367530dfca`;
- contract blob `16ab4ea07c41273e58b72f970d6bed383bbbfe33`;
- `service_dark` scalar semantic `BASE_LEVEL_R8_SCALAR_VALUES_ROW_MAJOR`;
- 512x512;
- scalar SHA-256 `b8d13c07f9b71278042b0d42d44b84579a3f327c6adf6723cae4c8c8f06dd38e`;
- R8 range `153..183`, 31 distinct values;
- Art Direction preference `PASS_ART_DIRECTION_OBJECT_SERVICE_DARK_BOUNDED_ROUGHNESS_MICROVARIATION_PREFERENCE_024`.

Exact Technical Art transport authority:

- Object Technical Art PR #28 head `1bcdbae786e02f3ca46a89e4e0ff608d74f364b4`;
- accepted path: **Object Technical Art selected roughness semantic UC transport**;
- exact selected scalar survives merged UC ORM green on both retained source-surface primitives;
- emitted GLB SHA-256 `9327291569a04bcb22016dc2ac4292499531b141a19e4bfceb291122e20ac3f4`;
- UC UV status `MEASURED` with no findings in that bounded transport;
- real Godot target result `PASS_OBJECT_SERVICE_DARK_SELECTED_ROUGHNESS_SCALAR_TO_CURRENT_UC_TEXTURED_GLB_TO_GODOT_FRONT_VIEWS`.

The older parallel selected-roughness workflow on the same Technical Art head is not treated as authority. The repaired semantic-transport workflow is the exact accepted path.

## Bounded observation

`atmosphere_current_world_object_selected_roughness_readiness_observe.gd` extends the already-accepted Nature-flutter current-world observer and changes no scene state. When the exact Object is emitted after the existing post-normal Environment indexing step, it inspects `Mesh.ARRAY_TEX_UV` on all five Object surfaces.

The receiver is required to report zero UV0 values on all five surfaces. If UV0 appears later, this HOLD proof fails instead of continuing to claim that the receiver is unready.

The observer then renders the same complete 17-state current-world evidence surface. The verifier requires:

- exact Materials selected-field identity;
- exact Technical Art semantic transport identity and exact emitted GLB bytes;
- exactly one current-world Object receiver;
- five exact Environment Object material surfaces;
- zero UV0 values on every surface;
- `environment_adoption=false`;
- all 68 newly observed real-scene frames byte-identical to the accepted retained world;
- all 1,224 Weather projected-width observations still inside the existing 0.05 px gate.

## Decision semantics

Expected result:

`HOLD_CURRENT_WORLD_OBJECT_SELECTED_ROUGHNESS_RECEIVER__EXACT_UV0_BINDING_NOT_PRESENT`

This is not a rejection of the selected roughness field. It is a receiving-boundary result.

The upstream field has exact Materials authority and a valid Technical Art transport. The current Map receiver, however, has no exact TEXCOORD_0 identity. Assigning a spatial roughness texture merely because the surface is named `service_dark` would invent placement and silently cross the Technical Art / Environment ownership boundary.

## Smallest next handoff

Before Environment can perform a real current-world selected-roughness A/B, one of these exact receiving paths must be proven:

1. **UV rebind to the current five-surface receiver:** Technical Art supplies an exact source-triangle/UV correspondence that Environment can attach without changing Object source/material authority; or
2. **validated transport receiver:** Environment proves the Technical Art GLB representation can replace or feed the current Map Object receiver with exact geometry/transform/material-role correspondence and no hidden scene drift.

Then Environment can render the selected roughness in the full world and re-check Object readability, Nature/Building composition and Weather continuity. Runtime storage (`L8`, atlas sizing, target-device cost) remains a separate decision after visual receiving identity exists.

## Reusable composition rule

`SPATIAL_MATERIAL_FIELD_REQUIRES_EXACT_RECEIVER_UV_IDENTITY_BEFORE_CURRENT_WORLD_ADOPTION`

A scalar digest proves **what values exist**. It does not prove **where those values land on a different receiver**. Environment must bind the spatial coordinate identity before claiming that a selected field has entered the world.

## Ownership / non-overlap

- Object Materials owns selected roughness meaning and scalar identity.
- Technical Art owns UV/material-channel/GLB transport identity.
- Environment owns current-world receiving composition, exact receiver binding, rollback and scene evidence.
- Runtime owns storage representation and target-device performance/adoption.
- Art Direction and independent Visual QA own preference and visual acceptance.
- `axm-create-me` remains coordination-only.

## Truth boundary

This lane does not author UVs, change Object geometry, change scalar PBR values, alter the selected roughness field, change Nature/Weather timing, accept the Technical Art representation as the Map default, adopt Runtime storage, infer Art/QA approval, establish CANON, or claim production readiness.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.
