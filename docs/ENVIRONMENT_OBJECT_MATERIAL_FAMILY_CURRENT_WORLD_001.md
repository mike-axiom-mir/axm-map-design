# Environment Object Material Family — Current World 001

## Bounded question

Can the already-authored Object source-role scalar PBR family be received by the exact current Map world without changing Object geometry/transform/scale or introducing the utility module, the new inner-lid experiment, articulation, Building, Nature, Weather, dressing, path, cameras or lighting?

## Exact parent

- Environment parent head: `72d4128b602e27c886a0731ddd670ec8c14aaa7e`.
- Parent result: `PASS_CURRENT_WORLD_NATURE_MATERIAL_FAMILY_STRUCTURE` + target-host PASS.
- Parent composition digest: `5d050b2d628f73d7e1b738e900eaba22f56ac7bcdd5fc46a5bf781e1d9058866`.
- Parent artifact: `10463255035`, SHA-256 `a4dbc0e2b8ef6f16bb846d0f639b7935b414ddfe4222463f397d11a16cfc0a45`.

## Exact Object inputs

Current Map Object structural identity remains source-owned:

- source head `d3fa10a270faae7925811f44f03381fe5c5d0215`;
- source SHA-256 `49b1f9ed9865893d6de6f1ec8f069576732df694853fde4e3fcff366de32644a`;
- current-world receiver remains `468 vertices / 812 triangles`.

Object Materials PR #6 supplies the unchanged base source-role material profile at exact current PR head `c85517446a769e0d5f880fc0e9e32f47124f7b5e` and profile SHA-256 `dc200229d6c25fa84063aa51f66103abc022efa54b2167e4432a5b47fc40360c`.

This Environment proof consumes only the five material IDs used by the already-present static host:

- `shell_coating` — `#3F484EFF`, metallic `0.42`, roughness `0.54`;
- `service_dark` — `#252B2FFF`, metallic `0.18`, roughness `0.66`;
- `hardware_steel` — `#9AA3A8FF`, metallic `0.88`, roughness `0.32`;
- `rubber_guard` — `#171A1CFF`, metallic `0.0`, roughness `0.86`;
- `interface_orange` — `#A96532FF`, metallic `0.20`, roughness `0.48`.

`module_housing` is deliberately not consumed because the utility module is not present in the current Map receiver.

The newest Object inner-lid review is also deliberately excluded. It is a separate bounded experiment and its current Godot workflow is not a prerequisite for this base-family receiving proof.

## Receiving contract

Environment rebuilds the exact Object host source only to recover source component/group triangle ranges, maps each source-owned component role through the exact Materials profile, and attaches a five-surface receiving partition to the already-present current-world Object receiver.

The receiver must retain the exact 468 vertices / 812 triangles and exact triangle ordering. All 812 triangles must be covered once and only once by the five host materials. The workflow fails closed on source/profile/head drift, partition overlap/gaps, utility-module introduction, inner-lid scope widening, parent drift or unrelated scene drift.

## Evidence surface

Pinned target host: Godot 4.7.2 GL Compatibility.

The real-host observer extends the exact current Nature-family world. It retains the same 17 dynamic states, two fixed cameras and both inherited Weather modes, producing 68 matched parent/candidate frame pairs. Every pair is compared directly at >1 8-bit RGB-channel step, while the inherited Weather projected-width gate remains active.

Runtime counters are retained only as diagnostics for Runtime / Optimization. Art Direction and Visual Observer / QA retain aesthetic acceptance.

## Truth boundary

A PASS proves only that the exact base Object source-role scalar material family can be received on the static west Object inside this exact current-world parent while declared unrelated state remains fixed.

It does **not** prove final Object material preference; the inner-lid material candidate; utility-module adoption; articulation or latch behavior; UVs, textures, decals, wear or measured coating correctness; arbitrary camera/renderer equivalence; target-device performance; gameplay/collision/navigation; CANON; production readiness; or Environment mastery.
