# Environment Building Source Replacement 001

Status: EXPERIMENTAL / exact receiving proof only.

This lane stacks on the exact accepted east-foreground Environment state at `cdac7d1316631b3b130d5e558de2aee462a21d40` and replaces only `proxy:building-pavilion` with the exact source-owned Building PR #2 `service-pavilion-001` plus its two fitted `utility-access-panel-001` instances.

## Preserved scene state

The retained seed remains 29. The west source sapling, accepted compact east tree, exact Weather field, object proxies, remaining Nature proxies, readable path, fixed cameras and proof-host lighting remain unchanged. Building Materials PR #3 is deliberately not consumed; this proof uses the existing neutral Environment source material so structural composition is not conflated with lookdev acceptance.

## Bounded checks

The receiving evaluator reruns the exact east-tree Environment prerequisite, then reruns Building's own source generator/evaluator from exact head `4faa769b406bf3ad0ba9489a77141c27f122ce51`. It requires the exact pavilion/panel source digests, 152 vertices / 228 triangles, two source-owned receiver fits, rejected Building negative controls, exact Map slot provenance, no hidden scale or rotation, source bounds inside the reserved slot, at least 0.35 m readable-path clearance, preserved minimum spacing and byte-semantic preservation of unrelated scene state.

The target-host workflow renders baseline and candidate through the existing pinned Godot 4.7.2 GL Compatibility Environment observer. Baseline already contains both source-owned Nature bodies plus Weather; candidate adds only the Building source mesh and removes only the Building proxy.

## Truth boundary

A PASS means only that this exact Building source can replace this exact proxy in this exact retained Environment scene while the declared cross-asset composition constraints and fixed target-host observation path remain valid. It does not prove Building Materials PR #3 acceptance, final architecture, engineering/code compliance, collision/navigation, gameplay, target-device runtime budgets, final Art Direction, CANON, production readiness or Environment/Building mastery.
