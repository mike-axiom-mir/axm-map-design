# Environment Building Planar-Role Quotient Receiving Rebind 001

## Bounded question

Building Geometry PR #13 proves that starting from the 604-vertex source-intent indexed render domain, removing **only** source hard-normal identity from the equivalence key yields exactly **312 material-role + exact-position + protected-split groups**. The reviewed Map consumer also stores 312 post-normal indexed vertices, but equal counts alone do not prove the same receiving partition.

This pass asks one Environment-owned question: in the real retained Building + Nature + Object + Weather world, does the reviewed 312-vertex Godot consumer occupy exactly the same material-role + retained-placement position partition as Geometry's 312-group hard-normal quotient, while preserving the distinction between source hard normals and consumer-generated normals?

No new Building representation is created.

## Exact inputs

- Geometry quotient head: `7dfb1153dc5f80bcbf1b48803f044236d4ebb030`.
- Geometry parent source-intent indexed head: `b9b4ab63e23b9756ab79597e86ecc41ea75ea8b7`.
- Geometry quotient SHA-256: `7d9e0babf605e31ecb3e4edc92d06bd5460cf52a27f02ccbf32bbae44464688f`.
- Prior reviewed Map consumer identity head: `55e10a4fa700fb81020a95bd5143deca6a09f208`.
- Consumer ID: `map-consumer:service-pavilion-001:planar-role-post-normal-indexed-001`.
- Active segmented rollback/default: `7713cbe5863c3bc38dabb6236eb4b393401224b6`.
- Retained Building placement: source `[0,7.2,0]` -> Godot `[0,0,-7.2]`.

Geometry quotient metrics remain 604 parent render vertices -> 312 quotient groups, 292 source render-vertex identities removed, 188 groups crossing source hard-normal boundaries, group sizes `1:124 / 2:84 / 3:104`, and per-role groups `200 / 24 / 36 / 36 / 16`.

## Receiving proof

The Godot observer extends the already-reviewed post-normal indexed receiver and records final indexed vertex positions, generated normals and index streams per Building material surface after the existing `SurfaceTool.generate_normals()` + `SurfaceTool.index()` path. The verifier rebuilds the exact Geometry quotient, applies the existing Map basis and placement transform, requires exact per-role position-set equality, re-renders the unchanged 17-state / 68-observation world, rechecks all 1,224 Weather-width observations, reproves the active-relative Runtime trade space, requires all new frames to be byte-identical to the prior reviewed 312-consumer frames, and rejects a deliberate source-normal-authority inflation mutation.

## Truth boundary

A PASS means the current 312 Godot receiver is not merely the same count as Geometry's quotient: in this retained real world it has the exact same material-role + retained-placement position partition. It does **not** mean the consumer preserves the 604 source hard-normal identities, consumer-generated normals are source normals, Geometry's diagnostic authorizes collapsing the source representation, the 312 consumer becomes default, `+180` primitives are target-device accepted, Technical Art has byte-exact normal/index transport, or Art/QA prefer this representation.

The active segmented Building remains rollback/default. `axm-create-me` remains coordination-only. The four AXM roots remain the merge gate.
