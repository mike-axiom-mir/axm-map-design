# Environment Building compact-v2 current-world receiving review 001

## Purpose

This Environment pass tests one exact source-owned Building receiving representation inside the strongest current multi-asset world. It does **not** change Building source authority and it does **not** make the compact representation the default.

The exact current-world parent is Map Environment head `7713cbe5863c3bc38dabb6236eb4b393401224b6`, where sampled Nature leaf micro-flutter is already proven in the accepted split-culling world while indexed Object, visible Object footprint cue, source-width Weather, route, cameras and lighting remain fixed.

## Exact Building donors

- Geometry compact-v2: `mike-axiom-mir/axm-building-design` head `16253e7dd2f8cd590667f9631e4b50fdfcc7280d`.
- Hard-Surface owner gate: head `35d0ba62d7e534b3cd00ac69e99386843ffa3f2e`.
- Materials exact compact-v2 rebind: head `4179aa1401f5a9114399e2f998c96809d4b8ed2e`.
- Semantic Building source remains `header-segmented-23`.
- Reference receiving identity remains `boundary-only-union-shell-001`.
- Explicit review candidate is `boundary-only-union-shell-conforming-compact-v2-001`.

The owner selection rule remains `EXPLICIT_RECEIVING_REPRESENTATION_ID_REQUIRED__NO_DEFAULT_OR_IMPLICIT_FALLBACK`. Historical or sibling PASS evidence does not transfer automatically across representation identities.

## Bounded Environment change

Environment changes only the Building receiving mesh representation:

- current-world segmented Building: `184 vertices / 276 triangles / 5 material surfaces`;
- compact-v2 review receiver: `1004 vertices / 2052 triangles / 5 material surfaces`.

The compact-v2 triangles are rebound through the exact 19 source-component material owners into the unchanged five Building material roles:

- `frame_galvanized`: 1164 triangles;
- `infill_coating`: 88;
- `roof_membrane`: 388;
- `slab_mineral`: 388;
- `utility_panel_ochre`: 24.

Environment reuses the already-authored current-world Building placement translation and requires assembled world bounds to remain unchanged. Nature, Object, footprint dressing, Weather, path, cameras and lighting must remain exact.

## Evidence gate

The target-host gate uses pinned Godot 4.7.2 GL Compatibility and compares the exact compact-v2 receiver against the exact leaf-flutter parent across all `17 states × 2 cameras × 2 Weather presentations = 68` real-scene frame pairs.

The gate requires:

- exact Geometry / Hard-Surface / Materials donor identities;
- exact current-world parent identity;
- exact five-family material scalar preservation;
- exact 19-owner material coverage;
- exact compact-v2 `1004v / 2052t` identity;
- all non-Building static target-host rows unchanged;
- dynamic sapling target-host rows unchanged;
- all `1,224` Weather projected-width measurements preserved under the existing `0.05 px` residual gate;
- stable per-mode/per-camera proof-host counters;
- retained runtime counter deltas rather than automatic performance acceptance;
- current-world raster change staying inside the exact Building Materials donor's existing `0.1%` full-frame `>1 LSB` bounded-continuity guard.

That `0.1%` threshold is not invented by Environment after viewing the result. It is inherited from the exact compact-v2 Materials receiving evidence and is used only as a bounded continuity guard, not as final Art Direction or Visual QA acceptance.

## Important comparison boundary

Building Runtime's favorable compact-v2 cost comparison is against the `1420v / 2884t` reference boundary shell. The current Map world instead uses a much smaller `184v / 276t` segmented receiver. This pass therefore measures the compact-v2 cost again in the actual current world and does **not** assume the isolated Runtime saving transfers downstream.

A current-world cost increase is a valid result and must be retained if observed.

## Authority / non-overlap

- Building Geometry owns the compact mesh construction.
- Building Hard Surface owns whether compact-v2 is an available receiving identity.
- Building Materials owns its exact five-family rebind and bounded isolated continuity evidence.
- Map Environment owns only the current-world receiving composition and comparison.
- Art Direction / Visual QA own visual preference.
- Runtime owns target-device performance acceptance.
- Technical Art owns transport/import acceptance.
- `axm-create-me` remains coordination-only.

## Non-claims

Even a green Environment gate does not establish compact-v2 as the semantic/default Building representation, arbitrary-view equivalence, smooth/generated-normal equivalence, UV/texture equivalence, Technical-Art transport acceptance, target-device FPS/CPU/GPU/VRAM/memory improvement, final visual preference, collision/navigation/gameplay, CANON, production readiness, or Environment mastery.

The four AXM roots — Truth, Agency / non-domination, Continuity, Wisdom before speed — remain the merge gate.
