# Environment Building planar-role hard-normal authority rebind 001

## Scope

This is an Environment / World Art receiving-only continuation on existing Map PR #24.

It does not create or rewrite Building geometry, source normals, Materials, Nature, Object, footprint dressing, Weather, route, camera or lighting state. It does not change the default Building receiver.

The bounded question is:

> Can the already-reviewed 312-vertex current-world Building consumer be explicitly rebound to Building Hard Surface PR #14's source-owned hard-normal authority policy without relabelling the consumer as source-equivalent or weakening the existing independent transport/runtime/visual gates?

## Exact source-owner return

Building Hard Surface PR #14 exact head:

`7b86b1a9da1ef8dc670ca01cf4918728e68ece92`

Policy:

`assets/service_pavilion_001_planar_role_hard_normal_authority_policy.json`

The source-owner policy classifies:

- 604 source-intent render classes as `SOURCE_AUTHORIZED_RENDER_EQUIVALENCE`;
- 312 role+position groups as `DERIVED_ATTRIBUTE_DROPPING_PARTITION_NOT_SOURCE_EQUIVALENT`;
- 292 source render-vertex identities removed;
- 188 quotient groups crossing source hard-normal boundaries;
- exact dropped source attribute: `EXACT_CARDINAL_HARD_NORMAL`.

The source policy forbids transferring source-normal equivalence from an equal count or exact role+position partition match.

## Existing Environment consumer

Consumer identity:

`map-consumer:service-pavilion-001:planar-role-post-normal-indexed-001`

Existing derivation:

`GODOT_POST_NORMAL_PER_MATERIAL_SURFACE_INDEX`

Storage:

- 312 vertices;
- 1008 indices;
- 336 triangles;
- 5 material surfaces.

The consumer explicitly owns a generated-normal/index domain and does not claim source-owner render equivalence or byte-exact post-index source-normal identity.

## Receiving evidence

This pass intentionally reuses the exact retained real-Godot current-world evidence from Environment head:

`9c1fb7b1a53f06a6a8c3c72ce3d1c3e5e1354d3f`

That retained evidence already proves:

- 17 dynamic states;
- 68 real Godot 4.7.2 observations;
- Building + Nature + indexed Object + visible footprint cue + Weather composition present;
- 1224 Weather projected-width observations;
- maximum Weather width residual below the existing 0.05 px gate;
- exact 312 material-role + transformed-position partition equality with the Geometry-owned quotient;
- source hard-normal identity not preserved;
- consumer-generated normals are not source normals;
- current consumer remains non-default;
- active segmented Building remains rollback.

Because the new Hard-Surface return changes authority/provenance only, this activation does not generate a fake new visual delta. The retained 68-frame real-scene set is rehashed and rebound to the new source-owner authority contract.

## Reusable receiving rule

`SOURCE_ATTRIBUTE_QUOTIENT_REQUIRES_EXPLICIT_DROPPED_ATTRIBUTE_DECLARATION_AND_CONSUMER_OWNED_REPLACEMENT_IDENTITY`

Meaning:

When a receiving representation is smaller because it drops a source-owned render attribute from the equivalence key, the receiver must name that dropped source attribute and name its own replacement storage/generation identity. Equal counts or exact position partitions do not silently transfer the source attribute's authority.

This remains a Map/Building receiving discipline. It is not automatically promoted into Universal Creation or Profession Fabric.

## Holds

Default adoption remains false.

Still separate:

- Technical Art exact transport/import evidence;
- Runtime representative target-device acceptance, including the retained +180 primitive residual;
- Art Direction preference;
- independent Visual QA;
- CANON and production readiness.

The active segmented rollback stays pinned at:

`7713cbe5863c3bc38dabb6236eb4b393401224b6`

The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
