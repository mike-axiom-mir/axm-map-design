# Environment — current-world Building header-segmentation receiving gate 001

## Purpose

This is a bounded Environment / World Art receiving proof inside the existing Map PR #24 lane. It does not create a second Environment branch and does not move Building or Materials knowledge into Map.

The exact parent is Environment Object-readability-dressing head `a29aa1e3d2260e8eb5ab2ac78a95d35ce131214c`, composition digest `8e22d33400effef66f5af79a19229cd5a4370d2a68563ecd1432f4ccb480b815`.

The only intended current-world change is the emitted representation of `source:building:service-pavilion-001`:

- Building Hard Surface source authority: `34124101e616c423c5a3ed5e122ddf09b98a1650`;
- source representation contract: `axm.building-header-segmentation/v0.1`;
- successor revision: `service-pavilion-001/interpenetration-free-header-segmentation-003`;
- Materials authority: `09a534d9d6d4cdaa1bab70cc2d01345b48d8fcc8`;
- six exact emitted header segment IDs are explicitly bound to the established `frame_galvanized` family;
- fallback/prefix material inference is forbidden.

## Why this is Environment work

Hard Surface has already proved the source-owned representation: the two long logical headers become six exact source-owned header segments, removing four positive-volume interpenetrations / `0.02592 m3` of double-covered component volume while preserving the exact occupied union, assembled bounds and receiver mounts. Materials has already proved the exact emitted-ID mapping in a dedicated Godot material proof.

Neither donor proof establishes that this representation survives the full dynamic current-world composition. Environment therefore owns the smallest next receiving gate: compose the exact successor into the current Building + Nature + Object + Weather world without changing any unrelated scene decision.

## Fixed current-world dependencies

The proof must preserve from the exact parent:

- all 17 dynamic Weather / moving-sapling states;
- both fixed cameras and both inherited Weather presentation modes;
- source-width Weather evidence and its `0.05 px` fidelity bound;
- exact west modular equipment-case source and its transform/scale;
- the pending Map-owned west Object receiver-footprint dressing exactly as authored;
- exact rear Nature source and `CULL_BACK` receiving behavior;
- readable path, placement and lighting;
- the accepted Building five-surface scalar family, including `infill_coating #59666DFF`.

The Object footprint cue remains perceptually **held**: composing another exact Building source representation on top of that branch does not grant the cue Art Direction or Visual QA adoption.

## Exact successor representation

Expected Building source facts:

- historical logical receiver: `19` boxes / `152` vertices / `228` triangles;
- exact successor emission: `23` boxes / `184` vertices / `276` triangles;
- source positive-volume intersections: `4`;
- historical positive double-covered volume: `0.02592 m3`;
- successor positive-volume intersections: `0`;
- occupied-union residual: `0.0 m3`;
- receiver-mount residual: `0.0 m`;
- successor topology: `0` boundary, non-manifold, orientation-conflict and degenerate edges/triangles with `276` outward / `0` inward triangles.

Expected material box counts after exact emitted-ID binding:

- `frame_galvanized`: `16` boxes;
- `infill_coating`: `3`;
- `roof_membrane`: `1`;
- `slab_mineral`: `1`;
- `utility_panel_ochre`: `2`.

The target renderer still receives five material surfaces; source representation ownership remains Building Hard Surface and material mapping ownership remains Building Materials.

## Real target-host gate

Pinned Godot `4.7.2` GL Compatibility renders all `17 states × 2 cameras × 2 Weather presentation modes = 68` exact parent/successor pairs.

The scoped target-host gate requires:

1. exact `184v / 276t / 5-surface` Building identity reaches the renderer;
2. Object source, Object footprint dressing, rear Nature `CULL_BACK` and Weather source-width fidelity remain present;
3. every one of the 68 successor frames is compared with the exact retained `a29aa1e...` parent frame;
4. no pair may contain an RGB channel difference greater than one 8-bit step (`>1/255`);
5. runtime counters are recorded only as diagnostic handoff evidence, never as target-device acceptance.

A PASS is therefore a receiving continuity result, not a claim that the new representation is visually superior. Its value is that Environment can remove a source-owned interpenetration defect without silently changing the world read.

## Ownership / handoffs

- **Building Hard Surface:** remains owner of the segmentation geometry, overlap/union proof and representation revision.
- **Building Materials:** remains owner of exact emitted-ID-to-surface mapping and all PBR values.
- **Environment:** owns only explicit composition of those exact donor identities into the current Map world.
- **Visual Observer / 3D Art Direction:** retain final current-world perceptual acceptance; the pending Object footprint cue is still held independently.
- **Runtime / Optimization:** receives any observed draw/object/primitive/buffer/texture counter delta. Environment does not certify it.
- **UC / Profession Fabric:** unchanged; no Building-specific source or material semantics move there.

## Non-claims

This gate does not establish a boolean-unioned or globally vertex-manifold pavilion, final normals/tangents/UVs/textures/decals/weathering, physical architectural validity, target-device CPU/GPU/FPS/memory/batching acceptance, collision/navigation/gameplay, final Object-dressing preference, arbitrary-camera/renderer equivalence, CANON, production/game readiness or Environment mastery.

The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, and Wisdom before speed.
