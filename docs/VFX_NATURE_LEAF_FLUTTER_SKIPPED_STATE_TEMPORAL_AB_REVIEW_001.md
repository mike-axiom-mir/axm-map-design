# VFX Nature leaf flutter — skipped-state temporal A/B review 001

## Scope

This is a bounded VFX / Atmosphere review surface for the exact four source states omitted by the retained clean proof-host Nature leaf-flutter wall-clock run. It does not change the accepted Nature source effect or the current-world receiver.

The input authority remains the retained clean `frame_post_draw` telemetry from Map VFX timing head `795d9e8862e895e506c756b9ea01cd6228fa7ab7` plus the separately rendered exact real-Godot source-state image bank bound to Nature VFX effect `ecade64227ba1d3d1faf029ca7188ea63c2560ec` and accepted receiver `7713cbe5863c3bc38dabb6236eb4b393401224b6`.

## Gap

The skipped-state consequence review made each omitted state inspectable as static previous / skipped / next imagery and deterministic raster differences. Art Direction / Visual QA still need a temporal surface to judge whether the actual missing state is perceptually meaningful at review speed. Pixel counts alone cannot answer that.

## Bounded improvement

For each exact clean skip, this review:

- selects a seven-authored-slot window centered on the skipped slot;
- takes the clean side from the verified **cadence-aligned clean** lossless review generated from retained measured `frame_post_draw` telemetry;
- takes the comparison side from the same exact real-Godot source-state images on the authored `31.25 ms` ideal schedule;
- creates a native-speed side-by-side lossless FFV1 clip with **clean on the left / ideal on the right**;
- creates a clearly labeled `4x` slow diagnostic copy by multiplying review playback time only;
- uses a 240 Hz review raster clock only to place the two VFR review streams on one comparison surface; this convenience raster clock is not timing authority and performs no motion interpolation;
- retains the exact skip identity and source lineage in a machine-readable manifest.

The four windows remain:

- `path_eye` slot `19`, skipped phase `3`;
- `elevated_oblique` slot `11`, skipped phase `11`;
- `elevated_oblique` slot `23`, skipped phase `7`;
- `elevated_oblique` slot `38`, skipped phase `6`.

Each window begins three authored slots before the skip and spans seven source-step intervals (`218.75 ms`) so the missing state is seen with neighboring motion rather than as an isolated still.

## Truth boundary

The retained clean JSON telemetry remains timing authority. The new side-by-side clips are **review media only**. The ideal side is an unobserved authored schedule reference, not runtime evidence. The `4x` slow media retimes review playback only and must not be described as source timing. The 240 Hz comparison raster duplicates/holds exact input frames to share a review clock; it does not create synthetic in-between states.

No interpolation, source amplitude change, source retiming, Weather change, camera change, material change, geometry change, gameplay behavior or physics behavior is introduced.

A temporal reviewer can make a skip easier to inspect, but it cannot decide whether the motion is natural, smooth, aesthetically preferred, production-ready or important enough to justify a future presentation policy. Those judgments remain Art Direction / Visual QA. Runtime retains target-device timing/performance authority.

## Expected result semantics

A PASS means the review package is fail-closed bound to the exact retained clean-vs-ideal evidence, all four known clean skips are represented in bounded temporal context, and both native and diagnostic review media were built.

A PASS does **not** mean the clean proof-host playback is visually accepted and does not authorize VFX to interpolate or retime the accepted Nature source.
