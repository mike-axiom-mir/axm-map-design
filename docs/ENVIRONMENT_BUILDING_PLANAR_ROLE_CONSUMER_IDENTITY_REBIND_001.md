# Environment Building planar-role consumer identity rebind 001

## Bounded question

Can the already-reviewed indexed planar-role Building remain the current Environment review target after Hard Surface explicitly declares a 604-group cardinal-hard-normal source split domain, without silently relabeling the existing 312-vertex Godot receiver as source-equivalent?

## Ownership boundary

- Building Hard Surface owns source representation `boundary-only-planar-role-rectangle-render-001` and source split intent at `0caa9ac9644f027350935240476bd0bf3bb66e18`.
- Map Environment owns only the receiving-world consumer identity, composition review target and rollback.
- Runtime owns storage / primitive / target-device cost evidence.
- Technical Art owns transport/import and receiver exactness.
- Art Direction and Visual QA own retained appearance acceptance.

## Explicit identity split

Source-owner intent:

- 1,008 triangle corners;
- 604 exact equivalence groups;
- same material role + exact position + exact cardinal hard normal + explicit protected split identity;
- 336 triangles / 5 material roles.

Current reviewed Environment consumer:

- `map-consumer:service-pavilion-001:planar-role-post-normal-indexed-001`;
- Godot post-normal per-material-surface indexing;
- 312 stored vertices / 1,008 indices / 336 triangles / 5 surfaces;
- consumer-generated normal/index domain;
- no claim of exact source-owner equivalence;
- no claim of post-index normal byte identity.

## Current-world proof requirement

The dedicated workflow must re-render the exact 17-state / 68-observation current world and preserve:

- Building planar-role consumer storage identity;
- Nature, indexed Object and visible footprint cue;
- source-width Weather sequence and all 1,224 projected-width observations under the existing 0.05 px residual gate;
- current active-relative Runtime trade space: -8,304 B observed buffer memory and +180 primitives;
- retained indexing raster boundary: <=55 changed pixels/frame, <=1 LSB, zero pixels above 1 LSB.

## Decision boundary

A PASS means only that Environment has removed the source/consumer provenance ambiguity while the exact reviewed multi-asset world still reproduces. It does not default-adopt the planar-role receiver. Active segmented remains rollback/default until Runtime/target-device acceptance of the residual primitive cost closes. The measured Technical-Art normal repack remains non-exact even though Art Direction and independent Visual QA have separately accepted the retained appearance of the 312-vertex consumer.

The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
