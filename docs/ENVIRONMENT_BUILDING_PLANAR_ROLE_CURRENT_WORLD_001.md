# Environment Building planar-role current-world review 001

Status: **EXPERIMENT / CURRENT-WORLD RECEIVING REVIEW ONLY**

This lane tests one exact source-owned Building render receiver inside the already accepted multi-asset current world. It does not change the Building semantic source, material scalars, Nature, Object, footprint cue, Weather, route, cameras or lighting.

## Why this lane exists

Art Direction prefers the cleaner manufactured planar/highlight language seen in Building compact-v2, but Environment measured compact-v2 as both visually non-neutral and materially more expensive than Map's actual active `184v / 276t` segmented receiver. Building Hard Surface PR #11 therefore produced a new render-only candidate intended to preserve exact occupied-union surface coverage and the current five material roles at much lower topology than compact-v2.

This is the first exact downstream current-world receiver test for that candidate. No upstream PASS transfers automatically.

## Exact pinned identities

- accepted current-world parent: Map Environment head `7713cbe5863c3bc38dabb6236eb4b393401224b6`;
- Building Hard Surface donor: PR #11 head `93f22e4eeb9bb32516d4b11f8d8bcf47d9792910`;
- Building Materials partition donor: PR #3 head `4179aa1401f5a9114399e2f998c96809d4b8ed2e`;
- Materials profile blob: `f7945f4c17b7720f176c1b0ac4e1baa298691b25`;
- semantic source: `header-segmented-23`;
- active Map receiver: `184v / 276t / 5 surfaces`;
- held compact-v2 review receiver: `1004v / 2052t / 5 surfaces`;
- new planar-role review receiver: `672v / 336t / 168 rectangles / 5 surfaces`;
- representation ID: `boundary-only-planar-role-rectangle-render-001`.

The new candidate is explicitly `SOURCE_OWNED_DERIVED_RENDER_RECEIVING_OPTION_NOT_DEFAULT`. It is render-only and is not collision, navigation, manufacturing or structural transport topology.

## Current-world proof contract

The evidence gate must:

1. rebuild the exact Hard-Surface donor from its pinned head;
2. preserve all 19 source-component material owners and all five current material roles;
3. reuse the exact existing current-world material scalars without retuning;
4. preserve Building placement/bounds and all unrelated Nature/Object/Weather/world state;
5. render all 17 inherited states in both fixed cameras and both Weather presentation modes = 68 candidate frames;
6. compare all 68 candidate frames directly against the active segmented receiver;
7. compare all 68 candidate frames directly against the retained compact-v2 current-world review;
8. preserve all 1,224 inherited Weather projected-width observations under the existing `0.05 px` residual gate;
9. measure proof-host draw/object/primitive/buffer/texture deltas against both active and compact receivers;
10. retain the visual deltas without converting them into automatic Art Direction or Visual-QA preference.

No visual-continuity threshold is relaxed or invented for this candidate. The output is review evidence: structural receiving may pass while visual preference remains with Art Direction / Visual QA.

## Ownership / handoff boundary

- Building Hard Surface owns the render-receiver identity and surface/provenance contract.
- Building Materials owns the five-role partition and scalar material values.
- Map Environment owns only the receiving composition and cross-asset current-world evidence.
- Runtime owns performance/cost acceptance.
- Art Direction / Visual QA own visual preference and perceptual acceptance.
- Technical Art owns transport/import acceptance.
- `axm-create-me` remains coordination only.

The four AXM roots — **Truth, Agency / non-domination, Continuity, Wisdom before speed** — remain the merge gate.

## Explicit non-claims

This lane does not establish default adoption, semantic-source replacement, UV/texture/tangent/normal-map equivalence, target-device performance, transport equivalence, collision/navigation/physics/gameplay validity, CANON, production readiness or Environment mastery.
