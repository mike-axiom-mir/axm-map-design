# Environment current-world Building-successor + Weather source-width composition 001

Status: evidence lane only. No CANON or production promotion.

## Bounded question

Can the exact Art Direction / Visual QA preferred Weather source-width presentation from Map VFX PR #25 be received inside the exact Environment PR #24 source-successor current world without changing Building source/material identity, Object, Nature, path, cameras, lighting, or the retained 17-state Weather/sapling schedule?

## Exact parents

- Environment source-successor parent: `43d89a7cac48e57ebede0db8fc9983e8144222a0`
- Environment parent composition digest: `f1507e8969a0343bcabad3078136a29f99d670b9261750e001f0ed500b4c8f6d`
- VFX source-width donor: `15a03b7c3ba3aaa7c0475ca1a3091c15581f559b`
- Weather source: `05b26c4e82bbe0a4de0ee7bee34179efc58b9719`, seed `44021`
- Building source successor: `57f66b1245812f0c3d402232a046b86c0b5c72d8`
- Building material profile: `e8dd0c33b9b2aea108194af57a8fe8de39c7e67bb86109af6dbf3895f22c010b`

The Environment parent retains the exact west Object source and migrated rear Nature source. The VFX donor contributes only the already-proven 36 source-authored Weather widths and their fixed-camera receiving method.

## Receiving rule

The composition tool fails closed unless both retained parent artifacts match their exact heads and PASS identities. It then requires every Environment Weather streak to match the VFX donor by exact retained state, streak ID, order and all non-width fields before copying only `source_width_px` plus its receiving binding. The Building source-successor receiving payload, all five current material values, Object, Nature, path, cameras and lighting remain unchanged.

The real-host observer is deliberately layered rather than copied into a new renderer implementation. CI checks out the exact VFX PR #25 head and uses its exact `atmosphere_current_world_weather_width_observe.gd` as the Weather-width implementation. The Environment wrapper overrides only static-source construction so the exact five-surface Building successor is rendered instead of the historical neutral Building path.

## Expected evidence

A valid target-host PASS must show in Godot 4.7.2 GL Compatibility:

- all 17 states and both fixed 1100×720 cameras;
- 34 thin-line control frames + 34 source-width candidate frames;
- all 34 control frames exactly reproduce the retained Environment source-successor parent frames;
- all 1,224 projected-width measurements remain within the VFX donor's 0.05 px receiving tolerance;
- Building successor identity, five-surface material profile, west Object source and migrated rear Nature source reach the host;
- control/candidate renderer counters remain stable per fixed camera within each presentation mode;
- near-plane clips remain explicit provenance rather than silently dropped streaks.

## Held boundaries

This lane does not repair or accept the existing Building `infill_coating` visual-hierarchy failure. It does not prove arbitrary camera/resolution Weather width fidelity, physical weather dimensions, final atmosphere, target-device performance, collision/navigation/gameplay, CANON, production readiness, or Environment mastery.

Map owns receiving composition. Weather/VFX own atmosphere semantics and source-width presentation. Building owns source topology and Materials owns surface values. Nature and Object keep their own source identities. Visual QA / Art Direction retain perceptual acceptance, Runtime retains performance acceptance, and the four AXM roots remain the merge gate.
