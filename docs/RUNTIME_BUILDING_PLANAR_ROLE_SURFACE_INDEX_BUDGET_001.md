# Runtime Building planar-role surface-index budget 001

Environment exact-head workflow `35182784756` already measured the fresh Hard-Surface planar-role Building in the real current world, so Runtime does not duplicate that lane. Its retained unindexed receiver is `336 triangles / 5 surfaces` and is a stable `+3,600 B` proof-host buffer-memory and `+180` RenderingServer-primitive delta versus the actual active segmented receiver, with no draw-call, object or texture-memory change.

This bounded Runtime follow-on changes only receiver storage after the exact five surfaces and final generated normals already exist: `SurfaceTool.index()` is applied independently per material surface. Triangle membership, hard-surface representation, generated normals, material roles/scalars, transform and all unrelated current-world state remain unchanged.

The exact indexed candidate is compared against both the retained Environment planar-role receiver and the exact active current-world receiver across all 68 matched state/camera/Weather-mode observations. The proof records stored vertices/indices, modeled position+normal+u32-index bytes, RenderingServer counters, and pixel-level A/B deltas against the exact unindexed planar-role frames. A deliberate mutation removes the required buffer reduction and must fail closed.

A green workflow proves only this exact proof-host storage reduction. It does not authorize Environment adoption or Art/Visual-QA preference, and it does not claim target-device CPU/GPU/FPS/VRAM/heap, collision/transport/manufacturing, CANON or production readiness.

`axm-create-me` remains coordination-only. The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
