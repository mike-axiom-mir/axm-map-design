# Runtime Building Material Submission Budget 001

Status: BOUNDED RUNTIME / OPTIMIZATION EVIDENCE

This lane is stacked exactly on Environment PR #24 head `48bc157ad9c3ecb7ef8e9fb08fb4721ac76013c5` and changes no Environment producer, Building geometry, material values, Weather, Nature, Object, camera, lighting, gameplay, Universal Creation, or `axm-create-me` product code.

## Gap

Environment PR #24 converged the exact five-surface Building response into the exact 17-state current world and retained real Godot 4.7.2 GL Compatibility control/candidate evidence. Its truth boundary correctly left Runtime cost unclaimed.

The bounded Runtime question is therefore:

> What exact proof-host submission/memory cost is introduced by replacing the one-surface neutral Building response with the exact five-surface Building response, while the same 152-vertex / 228-triangle Building geometry and every unrelated world state remain fixed?

## Exact retained donor

Environment artifact:

- ID `10447016718`;
- name `environment-current-world-building-material-001-48bc157ad9c3ecb7ef8e9fb08fb4721ac76013c5`;
- SHA-256 `aeda6e564ae8ac99906c61ac56a692792c4227f2539b0c453e66914b9d15f1ff`;
- exact Environment head `48bc157ad9c3ecb7ef8e9fb08fb4721ac76013c5`;
- proof runtime `Godot 4.7.2 GL Compatibility`;
- 17 states × 2 fixed cameras for neutral control and five-surface candidate.

The Runtime workflow downloads this exact retained artifact, verifies the archive digest, verifies its retained exact-head receipt, and derives the budget from the target-host observations. Runtime does not recreate or alter the visual producer.

## Measure-before control

Neutral Building receiving representation:

- one rendered Building surface/material response;
- logical Building geometry `152 vertices / 228 triangles`;
- `path_eye`: `20 draw calls / 20 objects / 5,992 primitives`;
- `elevated_oblique`: `27 / 27 / 7,750`;
- observed buffer memory `6,580,320 B`;
- observed texture memory `12,875,715 B`.

These counters are stable across all 17 retained states.

## Five-surface candidate

Exact material profile SHA-256:

`e8dd0c33b9b2aea108194af57a8fe8de39c7e67bb86109af6dbf3895f22c010b`

Exact five receiving surface roles remain:

- `frame_galvanized`;
- `infill_coating`;
- `roof_membrane`;
- `slab_mineral`;
- `utility_panel_ochre`.

The logical Building geometry remains exactly `152 vertices / 228 triangles`.

Observed target-host counters, stable across all 17 states:

- `path_eye`: `32 draw calls / 32 objects / 5,992 primitives`;
- `elevated_oblique`: `39 / 39 / 7,750`;
- observed buffer memory `6,580,320 B`;
- observed texture memory `12,875,715 B`.

## Exact delta

Both fixed cameras retain the same exact delta through all 17 states:

- draw calls: **`+12`**;
- objects-in-frame counter: **`+12`**;
- RenderingServer primitives: **`+0`**;
- observed buffer memory: **`+0 B`**;
- observed texture memory: **`+0 B`**.

Relative to the neutral proof-host counter set this is:

- `path_eye`: **`+60%`** draw/object counters;
- `elevated_oblique`: **`+44.44444444444444%`** draw/object counters.

This is a real proof-host submission-cost signal. It is not a target-device FPS, CPU/GPU-time, VRAM, allocator-residency, or renderer-independent batching claim.

The five-surface representation adds four logical Building surfaces over the neutral control. The observed `+12` counter delta must **not** be generalized into a universal `3x per extra surface` rule: this one scene contains lighting/shadow/render-pass behavior that is not decomposed by this evidence.

## Visual tradeoff handoff

Runtime changes no pixels. The exact parent A/B remains the visual evidence.

Environment reports that the five-surface candidate improves galvanized-frame and ochre service-panel separation while the roof/infill become substantially darker and can approach one near-dark mass in `path_eye`. Runtime therefore does not collapse material surfaces simply to reduce counters before Materials / 3D Art Director / Visual QA decide whether the visual hierarchy is preferred.

The exact parent image delta remains stable across all 17 states:

- `path_eye`: `67,221 / 792,000` changed pixels (`8.4875%`);
- `elevated_oblique`: `37,182 / 792,000` (`4.6946969697%`).

## Runtime decision

Scoped result:

`PASS_EXACT_BUILDING_FIVE_SURFACE_SUBMISSION_COST_CHARACTERIZED`

Budget decision:

`HOLD_RUNTIME_NEUTRALITY_CLAIM__FIVE_SURFACE_ADDS_12_SUBMISSION_SLOTS_IN_EXACT_PROOF_HOST`

This is not a rejection of the five-surface look. It means the candidate is no longer allowed to travel as if its runtime submission cost were neutral.

If Art Direction / Materials / Visual QA prefer the five-surface result, the next bounded Runtime experiment should compare one explicitly separate lower-submission receiving representation against this exact candidate and require real visual equivalence. Runtime should not rewrite source material semantics by assumption.

## Non-claims

This evidence does not establish:

- CPU frame time;
- GPU frame time;
- FPS;
- VRAM or heap residency;
- production allocator behavior;
- renderer-independent draw/pass decomposition;
- target-device budgets;
- final material preference;
- LOD/streaming policy;
- final lighting;
- gameplay/collision/navigation;
- CANON, production readiness, game readiness, or Runtime / Optimization mastery.

`axm-create-me` remains coordination-only. Map owns receiving composition; Building owns Building source/material semantics; Runtime owns only this bounded performance evidence. The four AXM roots remain the merge gate.
