# Environment — Building utility-panel material receiver readiness 001

## Why this lane exists

Technical Art has now closed the exact owner -> material-bearing GLB -> current UC observer -> real Godot import path for the Building utility-panel review material. That creates a new Environment receiving question: can the exact current 184-vertex / 276-triangle / five-surface `header-segmented-23` Building receiver consume that owner UV/image identity without Environment inventing a mapping?

This lane answers only that question. It is stacked on Environment PR #49 so the reviewed +20 mm Object service-frame successor stays unchanged. No scene geometry, Building placement, panel clearance, Object, Nature, Weather, camera or light is edited here.

## Pinned real-world evidence

Environment parent:

- review head `07f04aa9b5d1620907240acb12c97759fd7c77f4`;
- rendered scene donor head `7cceec9f3e55a71f51ec227301a92e1896ea2d74`;
- retained real-Godot artifact `10524764391` / SHA-256 `a6699ed54f7486640d468b995ca68320ae384dc406ef02ac880cdfa0fa131155`;
- four retained runtime packets, each with 17 current-world states, two Nature assets, articulated Object, current Building receiver, Environment dressing and Weather-width evidence;
- current Building receiver remains `184 vertices / 276 triangles / 5 surfaces` with material roles `frame_galvanized`, `infill_coating`, `roof_membrane`, `slab_mineral`, `utility_panel_ochre`;
- corrected utility-panel clearance state remains `PASS_CURRENT_WORLD_BUILDING_UTILITY_PANEL_CLEARANCE_REBIND_RECEIVER__VISUAL_OBSERVABILITY_CHARACTERIZED__ADOPTION_HELD`.

Technical Art donor:

- Map Technical Art PR #40 head `457c086d27f3a9c010b365fe75d54b8f812b01ef`;
- retained artifact `10527162239` / SHA-256 `d760a61cd95f756de7e0143096bb7923a0dfbaff0d480ccff9a17dfd19f0c70c`;
- Hard Surface head `fbfa3b47048755b45dac91451171d5511c8d4f47`;
- Geometry head `02944a9f10528a051603df3a6fd7b3183730773f`;
- Materials head `5f096369eee2ef44275ea8f1c7dc1b6e564e71c8`;
- exact GLB SHA-256 `dc65fec67aa6aba4e4a14895b38122ae89b6eafba7f256b07c1a7c9e344bbd68`;
- real Godot import state `PASS_TARGET_HOST_IMPORTED_MATERIAL_BEARING_GLB`;
- exact four UVs `[0.15625,0.03125]`, `[0.84375,0.03125]`, `[0.84375,0.96875]`, `[0.15625,0.96875]`;
- exact 512 x 512 embedded image with retained PNG SHA-256 `e932cdd94d370184c7361862d5064149cc193e3a8fd80b269cab6543c0919198` and imported RGBA8 SHA-256 `02f8f464eabc734a3be687a7706edf8b8f62ece834fa981c8c993fbb8227bb4b`.

## Exact receiver boundary

The active segmented Building emitter proves material-role binding, but its mesh construction path emits positions with `SurfaceTool.add_vertex`, generates normals, commits the surface, then binds the scalar Building material. It does not write `TEXCOORD_0` / UV0 or bind the Technical-Art image. The downstream utility-panel clearance layer changes only the exact panel vertex positions, explicitly holds material values, and delegates back into that same segmented builder.

The retained real-world runtime receipts agree with the implementation boundary: the current Building row carries exact structural, material-role, material-profile, segmentation and clearance identities, but no UV/image transport identity.

Therefore the matching role name `utility_panel_ochre` is necessary but not sufficient evidence for material-bearing world adoption.

## Result

Expected scoped state:

`HOLD_CURRENT_WORLD_BUILDING_UTILITY_PANEL_MATERIAL_RECEIVER__CURRENT_184V_RECEIVER_HAS_NO_EXACT_UV0_IMAGE_TRANSPORT_BINDING`

Reusable rule:

`EXACT_MATERIAL_ROLE_AND_PANEL_PLACEMENT_DO_NOT_AUTHORIZE_WORLD_TEXTURE_ADOPTION_WITHOUT_AN_EXACT_RECEIVER_UV0_IMAGE_TRANSPORT_BOUNDARY`

The verifier intentionally rejects a negative control that asks Environment to self-declare an exact UV/image binding without new owner evidence.

## Handoff

Technical Art / Geometry should expose an exact current-receiver UV0 + image-binding handoff for the two utility-panel surface instances while preserving:

- the current `184v / 276t / 5-surface` Building identity;
- the corrected front/east panel placement;
- the existing five material-role partition;
- unrelated Object, Nature, Weather, route, cameras and lighting.

Once that exact boundary exists, Environment can perform a real current-world A/B using the owner material transport without inventing planar UVs or replacing the Building receiver with the four-vertex proof carrier.

## Truth boundary

This HOLD is not a claim that the material looks bad or that the upstream GLB is incomplete. Upstream transport is green. The missing evidence is specifically the exact mapping from that owner transport into the current Environment receiver. No automatic Environment adoption, Art/QA acceptance, Runtime/device acceptance, CANON, gameplay, collision/navigation or production readiness is claimed.

`axm-create-me` remains coordination-only. The four AXM roots remain the merge gate: Truth, Agency / non-domination, Continuity, Wisdom before speed.
